# Add the Nim toolchain root so that `import compiler/nimeval` resolves.
# $nim = directory containing the nim executable (e.g. <toolchain>/bin)
# $nim/.. = toolchain root, which contains compiler/, lib/, etc.
path:"$nim/.."
